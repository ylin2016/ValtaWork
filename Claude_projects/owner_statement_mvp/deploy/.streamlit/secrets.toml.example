# Copy to .streamlit/secrets.toml (local) OR paste into Streamlit Cloud → App → Settings → Secrets.
# REQUIRED for a public demo: this password gates access to real owner data.
app_password = "change-me-before-deploying"
